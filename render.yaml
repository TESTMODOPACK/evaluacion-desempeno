databases:
  # Base de Datos PostgreSQL
  - name: evaluacion-desempeno-db
    databaseName: evaluacion
    user: eval_user
    plan: free
    region: ohio

services:
  # Base de Datos Redis (Caché)
  - type: keyvalue
    name: evaluacion-desempeno-redis
    plan: free
    region: ohio
    maxmemoryPolicy: noeviction
    ipAllowList: []

  # Backend (NestJS)
  - type: web
    name: evaluacion-desempeno-api
    env: node
    plan: free
    region: ohio
    rootDir: apps/api
    buildCommand: pnpm install && npx turbo run build --filter=@repo/api
    startCommand: pnpm run start:prod
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: evaluacion-desempeno-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: keyvalue
          name: evaluacion-desempeno-redis
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: "3001"
      # Front end URL para CORS
      - key: FRONTEND_URL
        value: "https://TU-SITIO.netlify.app"
